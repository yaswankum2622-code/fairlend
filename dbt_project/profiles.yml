fairlend:
  target: dev
  outputs:
    dev:
      type: sqlite
      threads: 1
      database: "../../data/fairlend.db"
      schema: main
      schemas_and_paths:
        main: "../../data/fairlend.db"
