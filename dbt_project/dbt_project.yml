name: fairlend
version: '1.0.0'
config-version: 2
profile: fairlend
model-paths: ["models"]
test-paths: ["tests"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]
models:
  fairlend:
    staging:
      +materialized: view
    marts:
      +materialized: table
